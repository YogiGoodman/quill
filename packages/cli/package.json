{
  "name": "@quill/cli",
  "version": "0.1.0",
  "private": true,
  "description": "The `quill` command-line interface.",
  "license": "ISC",
  "type": "module",
  "bin": {
    "quill": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@quill/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  }
}
